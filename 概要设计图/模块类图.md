# 熙心健康体检平台 - 模块类图

## 模块划分概览

```
                    熙心健康体检平台
                         |
    +----+----+----+----+----+----+----+----+
    |    |    |    |    |    |    |    |    |
  用户  订单  预约  咨询  报告  体检  运营  体检  质控
  管理  管理  管理  管理  管理  套餐  管理  数据  管理
                        管理        分析
```

## 1. 用户管理模块 (User Management)

```plantuml
@startuml UserManagement
skinparam classAttributeIconSize 0
skinparam linetype ortho

class User {
  -Long id
  -String userNo
  -String name
  -String phone
  -String passwordHash
  -Integer authStatus
  -Integer isVip
  --
  +login() bool
  +register() void
  +getInfo() User
  +updateInfo() void
}

class StaffAccount {
  -Long id
  -String username
  -String passwordHash
  -String displayName
  -Integer status
  -String roleCode
  -Long roleId
  --
  +login() bool
  +getInfo() StaffAccount
  +updateStatus() void
}

class StaffRoleRel {
  -Long id
  -Long staffId
  -Long roleId
  -Integer status
  --
  +assign() void
  +remove() void
}

class RoleType {
  -String code
  -String name
  -Integer status
  --
  +values() List<RoleType>
}

' 关系说明
User "1" -- "0..*" StaffAccount : 后台账号关联用户
StaffAccount "1" -- "0..*" StaffRoleRel : 角色绑定
StaffRoleRel "*" -- "1" RoleType : 角色类型
@enduml
```

### 关系说明
| 关系 | 类型 | 说明 |
|------|------|------|
| User → StaffAccount | 关联 | 后台账号关联普通用户 |
| StaffAccount → StaffRoleRel | 组合 | 账号包含多个角色绑定 |
| StaffRoleRel → RoleType | 聚合 | 角色绑定关联角色类型 |

---

## 2. 预约管理模块 (Appointment Management)

```plantuml
@startuml AppointmentManagement
skinparam classAttributeIconSize 0
skinparam linetype ortho

class Appointment {
  -Long id
  -String appointmentNo
  -Long userId
  -Long packageId
  -String centerCode
  -LocalDate appointDate
  -String timeSlotCode
  -Integer status
  --
  +create() Appointment
  +cancel() void
  +getDetail() Appointment
  +getAvailableSlots() List
}

class ExamPackage {
  -Long id
  -String packageCode
  -String packageName
  -String category
  -BigDecimal price
  -Integer status
  --
  +getList() List
  +getDetail() ExamPackage
  +create() void
  +update() void
}

class ExamPackageItem {
  -Long id
  -Long packageId
  -String itemCode
  -String itemName
  -String departmentCode
  -Integer status
  --
  +create() void
  +update() void
}

class ResourceCapacity {
  -Long id
  -String centerCode
  -LocalDate capacityDate
  -String timeSlotCode
  -Integer capacityTotal
  -Integer capacityUsed
  -Integer status
  --
  +create() void
  +updateStatus() void
  +getAvailable() ResourceCapacity
}

' 关系说明
Appointment "*" -- "1" ExamPackage : 选择套餐
ExamPackage "1" *-- "0..*" ExamPackageItem : 套餐包含项目
Appointment "*" -- "1" ResourceCapacity : 预约时段
@enduml
```

### 关系说明
| 关系 | 类型 | 说明 |
|------|------|------|
| Appointment → ExamPackage | 聚合 | 预约选择体检套餐 |
| ExamPackage → ExamPackageItem | 组合 | 套餐包含多个体检项目 |
| Appointment → ResourceCapacity | 关联 | 预约占用资源时段 |

---

## 3. 订单管理模块 (Order Management)

```plantuml
@startuml OrderManagement
skinparam classAttributeIconSize 0
skinparam linetype ortho

class Order {
  -Long id
  -String orderNo
  -Long userId
  -Long appointmentId
  -Long packageId
  -BigDecimal totalAmount
  -BigDecimal discountAmount
  -BigDecimal payAmount
  -Integer status
  --
  +create() Order
  +pay() void
  +refund() void
  +getDetail() Order
  +getList() List
}

class OrderItem {
  -Long id
  -Long orderId
  -Integer itemType
  -String refItemCode
  -String refItemName
  -Integer qty
  -BigDecimal unitPrice
  -BigDecimal amount
  --
  +create() void
  +update() void
}

class PaymentRecord {
  -Long id
  -Long orderId
  -String paymentNo
  -BigDecimal amount
  -String payChannel
  -Integer status
  --
  +create() void
  +queryStatus() PaymentRecord
}

class RefundApply {
  -Long id
  -Long orderId
  -String refundNo
  -BigDecimal refundAmount
  -String reason
  -Integer status
  --
  +apply() RefundApply
  +audit() void
}

' 关系说明
Order "1" *-- "0..*" OrderItem : 订单明细
Order "1" -- "0..*" PaymentRecord : 支付记录
Order "1" -- "0..*" RefundApply : 退款申请
@enduml
```

### 关系说明
| 关系 | 类型 | 说明 |
|------|------|------|
| Order → OrderItem | 组合 | 订单包含多个订单项 |
| Order → PaymentRecord | 聚合 | 订单关联支付记录 |
| Order → RefundApply | 关联 | 订单关联退款申请 |

---

## 4. 体检执行模块 (Exam Execution)

```plantuml
@startuml ExamExecution
skinparam classAttributeIconSize 0
skinparam linetype ortho

class ExamTask {
  -Long id
  -String taskNo
  -Long appointmentId
  -Long orderId
  -Long userId
  -Long packageId
  -Integer taskStatus
  -Integer reportStatus
  --
  +generate() ExamTask
  +getDetail() ExamTask
  +getGuide() List
}

class ExamTaskItem {
  -Long id
  -String taskItemNo
  -Long taskId
  -String itemCode
  -String itemName
  -String departmentCode
  -Long doctorId
  -Integer itemStatus
  --
  +start() void
  +complete() void
  +getDetail() ExamTaskItem
}

class ExamResult {
  -Long id
  -String resultNo
  -Long taskId
  -Long taskItemId
  -Long userId
  -String resultType
  -String resultValue
  -BigDecimal resultNumber
  -Integer isAbnormal
  -Integer abnormalLevel
  -Long entryDoctorId
  --
  +create() ExamResult
  +getList() List
}

class ExamDepartmentRoute {
  -Long id
  -String departmentCode
  -String departmentName
  -Integer routeSort
  -Integer status
  --
  +create() void
  +update() void
}

' 关系说明
ExamTask "1" *-- "0..*" ExamTaskItem : 任务拆分
ExamTaskItem "1" -- "0..*" ExamResult : 录入结果
ExamTaskItem "*" -- "1" ExamDepartmentRoute : 科室路由
@enduml
```

### 关系说明
| 关系 | 类型 | 说明 |
|------|------|------|
| ExamTask → ExamTaskItem | 组合 | 体检任务拆分为多个任务项 |
| ExamTaskItem → ExamResult | 聚合 | 任务项关联体检结果 |
| ExamTaskItem → ExamDepartmentRoute | 关联 | 任务项关联科室路由 |

---

## 5. 报告管理模块 (Report Management)

```plantuml
@startuml ReportManagement
skinparam classAttributeIconSize 0
skinparam linetype ortho

class ExamReport {
  -Long id
  -String reportNo
  -Long appointmentId
  -Long taskId
  -Long userId
  -Long packageId
  -Long templateId
  -LocalDate reportDate
  -String overallConclusion
  -Integer status
  --
  +generate() ExamReport
  +review() void
  +publish() void
  +withdraw() void
  +getDetail() ExamReport
  +getList() List
}

class ExamReportItem {
  -Long id
  -Long reportId
  -Long resultId
  -String itemCode
  -String itemName
  -String resultValue
  -Integer isAbnormal
  --
  +create() void
  +getList() List
}

class DoctorReviewRecord {
  -Long id
  -Long reportId
  -Long doctorId
  -Integer reviewStatus
  -String reviewComment
  --
  +create() void
  +getList() List
}

class ReportTemplate {
  -Long id
  -String templateCode
  -String templateName
  -Integer status
  --
  +create() void
  +update() void
  +getList() List
}

class ReportSectionTemplate {
  -Long id
  -Long templateId
  -String sectionCode
  -String sectionName
  -Integer sortOrder
  --
  +create() void
  +update() void
}

' 关系说明
ExamReport "1" *-- "0..*" ExamReportItem : 报告明细
ExamReport "1" -- "0..*" DoctorReviewRecord : 审核记录
ExamReport "*" -- "1" ReportTemplate : 使用模板
ReportTemplate "1" *-- "0..*" ReportSectionTemplate : 模板章节
@enduml
```

### 关系说明
| 关系 | 类型 | 说明 |
|------|------|------|
| ExamReport → ExamReportItem | 组合 | 报告包含多个报告项 |
| ExamReport → DoctorReviewRecord | 聚合 | 报告关联审核记录 |
| ExamReport → ReportTemplate | 关联 | 报告使用报告模板 |
| ReportTemplate → ReportSectionTemplate | 组合 | 模板包含多个章节 |

---

## 6. 咨询管理模块 (Consultation Management)

```plantuml
@startuml ConsultationManagement
skinparam classAttributeIconSize 0
skinparam linetype ortho

class DoctorConsultation {
  -Long id
  -String consultationNo
  -Long userId
  -Long doctorId
  -String doctorName
  -Long reportId
  -String consultationType
  -String consultationTitle
  -String consultationContent
  -Integer consultationStatus
  -Integer priorityLevel
  --
  +create() DoctorConsultation
  +assign() void
  +close() void
  +getDetail() DoctorConsultation
  +getList() List
}

class DoctorConsultationReply {
  -Long id
  -Long consultationId
  -Long replyUserId
  -String replyContent
  -Integer replyType
  --
  +create() void
  +getList() List
}

' 关系说明
DoctorConsultation "1" *-- "0..*" DoctorConsultationReply : 咨询回复
DoctorConsultation "*" -- "0..1" ExamReport : 关联报告
@enduml
```

### 关系说明
| 关系 | 类型 | 说明 |
|------|------|------|
| DoctorConsultation → DoctorConsultationReply | 组合 | 咨询包含多个回复 |
| DoctorConsultation → ExamReport | 关联 | 咨询关联体检报告 |

---

## 7. 体检数据分析模块 (Data Analysis)

```plantuml
@startuml DataAnalysis
skinparam classAttributeIconSize 0
skinparam linetype ortho

class ReportCompareTask {
  -Long id
  -String taskNo
  -Long userId
  -Long baselineReportId
  -Long compareReportId
  -Integer status
  --
  +create() ReportCompareTask
  +getDetail() ReportCompareTask
}

class ReportCompareResult {
  -Long id
  -Long taskId
  -String itemCode
  -String baselineValue
  -String compareValue
  -Integer changeType
  --
  +create() void
  +getList() List
}

class IndicatorTrendTag {
  -Long id
  -Long userId
  -String itemCode
  -Integer trendType
  -Integer yearCount
  --
  +create() void
  +getList() List
}

class HealthRiskScore {
  -Long id
  -Long taskId
  -Long userId
  -BigDecimal scoreTotal
  -BigDecimal scoreAbnormal
  -BigDecimal scoreTrend
  -Integer riskLevel
  --
  +calculate() void
  +getList() List
}

class HealthAdviceRecord {
  -Long id
  -String adviceNo
  -Long userId
  -Long reportId
  -Long compareTaskId
  -String adviceType
  -Integer riskLevel
  -String adviceContent
  --
  +create() void
  +getList() List
}

' 关系说明
ReportCompareTask "1" *-- "0..*" ReportCompareResult : 对比结果
ReportCompareTask "1" -- "0..1" HealthRiskScore : 风险评分
ReportCompareTask "1" -- "0..*" HealthAdviceRecord : 健康建议
ReportCompareTask "*" -- "1" IndicatorTrendTag : 指标趋势
@enduml
```

### 关系说明
| 关系 | 类型 | 说明 |
|------|------|------|
| ReportCompareTask → ReportCompareResult | 组合 | 对比任务包含多个对比结果 |
| ReportCompareTask → HealthRiskScore | 关联 | 对比任务关联风险评分 |
| ReportCompareTask → HealthAdviceRecord | 聚合 | 对比任务生成健康建议 |
| ReportCompareTask → IndicatorTrendTag | 关联 | 对比任务关联指标趋势 |

---

## 8. 跨模块关系总览

```plantuml
@startuml CrossModuleRelations
skinparam classAttributeIconSize 0
skinparam linetype ortho

' 用户管理
class User
class StaffAccount

' 预约管理
class Appointment
class ExamPackage

' 订单管理
class Order
class OrderItem

' 体检执行
class ExamTask
class ExamTaskItem
class ExamResult

' 报告管理
class ExamReport
class ExamReportItem
class DoctorReviewRecord

' 咨询管理
class DoctorConsultation

' 数据分析
class ReportCompareTask
class HealthRiskScore
class HealthAdviceRecord

' ====== 用户管理模块 ======
User "1" -- "0..*" Appointment : 创建预约
User "1" -- "0..*" Order : 创建订单
User "1" -- "0..*" ExamReport : 查看报告
User "1" -- "0..*" DoctorConsultation : 发起咨询

' ====== 预约管理模块 ======
Appointment "0..1" -- "1" Order : 生成订单
Appointment "0..1" -- "1" ExamTask : 生成体检任务

' ====== 订单管理模块 ======
Order "1" *-- "0..*" OrderItem : 订单明细

' ====== 体检执行模块 ======
ExamTask "1" *-- "0..*" ExamTaskItem : 任务拆分
ExamTaskItem "1" -- "0..*" ExamResult : 录入结果
ExamResult "0..*" -- "1" ExamReportItem : 关联报告项

' ====== 报告管理模块 ======
ExamReport "1" *-- "0..*" ExamReportItem : 报告明细
ExamReport "1" -- "0..*" DoctorReviewRecord : 审核记录
ExamReport "1" -- "0..1" ExamTask : 关联体检任务

' ====== 咨询管理模块 ======
DoctorConsultation "1" -- "0..1" ExamReport : 关联报告

' ====== 数据分析模块 ======
ReportCompareTask "1" -- "1" ExamReport : 基线报告
ReportCompareTask "1" -- "1" ExamReport : 对比报告
ReportCompareTask "1" -- "0..1" HealthRiskScore : 风险评分
ReportCompareTask "1" -- "0..*" HealthAdviceRecord : 健康建议

' StaffAccount 不直接参与核心业务流
StaffAccount "1" -- "0..*" ExamResult : 医生录入
StaffAccount "1" -- "0..*" DoctorReviewRecord : 医生审核
StaffAccount "1" -- "0..*" DoctorConsultation : 医生回复
@enduml
```

---

## 模块职责总结

| 模块 | 核心职责 | 主要实体 |
|------|----------|----------|
| 用户管理 | 用户注册登录、后台账号管理、角色权限 | User, StaffAccount, StaffRoleRel |
| 预约管理 | 预约创建/取消、套餐选择、资源时段管理 | Appointment, ExamPackage, ResourceCapacity |
| 订单管理 | 订单创建/支付/退款、订单明细 | Order, OrderItem, PaymentRecord, RefundApply |
| 体检执行 | 体检任务生成、任务项执行、结果录入 | ExamTask, ExamTaskItem, ExamResult |
| 报告管理 | 报告生成/审核/发布、模板管理 | ExamReport, DoctorReviewRecord, ReportTemplate |
| 咨询管理 | 咨询创建/分配/回复、咨询记录 | DoctorConsultation, DoctorConsultationReply |
| 数据分析 | 报告对比、指标趋势、风险评分、健康建议 | ReportCompareTask, HealthRiskScore, HealthAdviceRecord |
| 运营管理 | 排班管理、资源容量、运营统计 | ResourceCapacity, StatDailyReport |
| 质控管理 | 体检质量控制、异常监控 | ExamResult, DoctorReviewRecord |
